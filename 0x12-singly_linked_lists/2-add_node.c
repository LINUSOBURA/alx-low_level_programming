#include "lists.h"
list_t *add_node(list_t **head, const char *str)
{
	list_t *createNewNode(char *str) = (list_t *)malloc(sizeof(Node));
	if (newNode == NULL)
		return (NULL);
	newNode->str = str;
	newnode->next = NULL;
	return (newNode);



}
